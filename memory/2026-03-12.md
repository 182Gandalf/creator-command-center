# Daily Log — March 12, 2026

## Session Summary (07:00–13:54 UTC)

### FlowCast — Major Build Day

**DB & Infrastructure**
- Ran `alembic upgrade head` locally (Railway DB already up to date — tables existed)
- Fixed pytrends `urllib3<2` compatibility issue — pinned in requirements.txt, pushed
- Scheduler: increased trend fetch delay from 10s → 60s+jitter, moved from 06:00 → 03:00 UTC
- Manually fetched fitness trends (10 topics saved to DB for Daz's test account)

**Trends Tab**
- Wired up `/api/trends/current` to return full 10 topics with `?limit=` param + `fetched_at`
- Built live Trends tab in dashboard-new.html: score bars, meta timestamp, empty state
- Added Splash gating: trend #1 visible, #2–10 blurred with lock overlay + "Upgrade to Creator" CTA

**Phase 5: Email Sequences — COMPLETE**
- `services/email.py` — Resend SDK wrapper, FROM display name, plain-text + headers support
- `services/digests.py` — `generate_digest_scripts()` (Gemini OpenAI-compatible), `generate_and_send_trend_digest()`, Day 7 email
- `services/scheduler.py` — 3 new jobs: Creator weekly digest (Mon 07:00), Studio daily digest (07:00), Day 7 hourly check
- Admin endpoints: `/api/admin/test/send-digest`, `/api/admin/test/send-day7-email`
- Successfully triggered test digest to `darren.stegmann@gmail.com` (Fitness, Creator tier) ✅
- Successfully triggered Day 7 email to `stgdar001@gmail.com` (Fitness, Splash tier) ✅
- DIGEST_API_KEY was initially a suspended Gemini key — Daz replaced it, now working

**Onboarding Fix**
- Splash users couldn't submit onboarding — blocked by `contentTypes.length === 0` check even when Q7 was disabled
- Fix: frontend checks `input.disabled` before validating; backend made Q6–10 Optional in Pydantic model
- Validation and save logic conditional on fields being provided

**OG Image**
- Generated new 1200×630 OG image (replaced square 1024×1024 logo-only)
- First attempt: custom Pillow generation (7.5/10, slightly left-biased)
- Second attempt: used Daz's brand reference JPG directly (1280×634 → cropped to 1200×630) ✅
- Added `?v=3` cache bust to all OG meta tags in index.html + pricing.html
- Email headers updated: actual logo image + FlowCast wordmark (both digest + Day 7)

**Pricing Page**
- Added "Trend Intelligence" section to all 3 plans
- Splash: 1 topic preview, locked indicators
- Creator: Full 10 topics + weekly digest email
- Studio: Full 10 topics + daily digest + daily alerts

**Email Deliverability**
- Day 7 email rewritten as plain personal email (no template, no buttons, no images)
- Added plain-text versions to ALL emails
- Added `List-Unsubscribe` headers to digest emails
- Subject line cleanup: Day 7 → "One week in — a quick note about your FlowCast"
- FROM display name: `Daz from FlowCast <hello@flowcast.space>`
- `FROM_NAME` env var needed in Railway
- DNS check: DMARC already `p=quarantine` ✅, DKIM configured ✅, SPF missing Resend (`include:amazonses.com` needs adding in Cloudflare)

**PLAN.md**
- Removed all launch date targets — "ship when ready" philosophy adopted

### Plans / Reminders Logged to DASHBOARD.md
- TRENDS-001: SerpAPI migration (trigger: first paying user, $25/mo)
- TRENDS-002: Extended trend data (realtime, interest_over_time, trending_searches, interest_by_region) — after TRENDS-001

### Commits Today
- `f43f7a5` — pin urllib3<2
- `a208db3` — scheduler 60s+jitter, 3AM UTC
- `0fcc86c` — Phase 5 email sequences
- `6edc7b0` — admin send-digest endpoint
- `6faff41` — diagnostic send-digest endpoint
- `9cb10c1` — deep diagnostics
- `1fdee53` — fix generate_ai_response() call signature
- `35d8686` — OG image v1 + logo in email headers
- `604863d` — OG image v2 (Pillow centred)
- `b06e260` — OG image v3 (brand reference direct crop)
- `4138896` — OG ?v=3 cache bust
- `4f2f05c` — onboarding Q6-10 optional fix
- `f7c6b92` — admin day7 email endpoint
- `148bcaf` — Splash gating: trends lock, onboarding overlay, pricing Trend Intelligence
- `4791e40` — deliverability: plain text, personal Day 7, List-Unsubscribe, clean subjects

### Key Decisions
- No fixed launch date — ship when ready (Daz's call, March 12)
- SerpAPI at $25/mo is the planned pytrends replacement when first paying user arrives
- Day 7 email deliberately plain/personal — no HTML marketing template for Primary inbox placement
- DMARC already at p=quarantine — SPF fix (Cloudflare, add `include:amazonses.com`) is only remaining DNS gap

## Evening Review (20:00 UTC)

**Status:** No new work after 16:00 UTC. MEMORY.md already updated at 16:00 with full day's context. Memory is current.

**Outstanding items carried to tomorrow:**
- SPF record: add `include:amazonses.com` in Cloudflare DNS (TXT) — one small Cloudflare action
- FROM_NAME env var: add to Railway dashboard
- flowcast-daily-improvements.md analysis cron appears stale (last updated Feb 27) — needs investigation
- Hero headline typo on flowcast.space: "aboutwhat" → "about what" — trivial fix, not yet confirmed done

---

## Session Continued (14:00–20:16 UTC) — Major Feature Sprint

### Commits This Session (8 commits)
- `e562cb3` — White-label PDF exports (Studio): reportlab, brand settings modal, hooks/scripts/content-pack PDFs
- `83a61d1` — Day 7 email copy: added retention paragraph after "Creator-tier FlowCast learns all of that."
- `703322d` — BUG FIX: ideas counter reading wrong table (User vs CreatorProfile.splash_ideas_used_this_month)
- `68ed1f3` — Animated logos: replaced all static logo.jpg/<img> with <video autoplay loop muted> across 13 templates
- `964ecb0` — Studio plan: calendar PDF export (reportlab), workspace switcher (5 slots, localStorage), 90-day calendar lock UI
- `70719d1` — Zapier webhook: api_keys table, routers/settings.py, routers/zapier.py, settings.html Integrations section
- `3558a53` — Zapier webhook updated to return both saved scripts AND ideas (two arrays)
- `ea76216` — FAQ + Help Guide pages: /faq (35 questions, 8 sections, live search), /help (full tutorial, sticky TOC)

### Key Bugs Fixed
- **Ideas counter always showed 20**: `User.splash_ideas_used_this_month` was never written to — the ideas router writes to `CreatorProfile.splash_ideas_used_this_month`. Fixed profile API to read from CreatorProfile.

### Key Features Added
**White-label PDF exports (Studio):**
- 3 new columns on creator_profiles: brand_name, brand_color, brand_logo_url
- Migration: 2024_03_13_brand_settings.py
- New router: routers/export.py — GET/POST /api/export/branding, GET /api/export/pdf/{scripts,hooks,content-pack}
- Dashboard: Brand Settings modal with live color preview, sidebar button (Studio only)
- PDF export bars on Saved > Hooks and Saved > Scripts tabs

**Studio plan completions (audit + build):**
- 90-day calendar: already in backend (_get_tier_days("studio") = (90,90)); added visible lock banner for non-Studio
- Daily trend digest: confirmed working (studio_daily_digest scheduler)
- Workspace switcher: 5 slots for Studio, 1 for others; locked slots visible with 🔒; localStorage-based; add/rename/delete
- Calendar PDF export: GET /api/calendar/export/pdf — Studio only, branded with CreatorProfile

**Zapier webhook:**
- New table: api_keys (id, user_id, api_key unique 32-char, created_at)
- Migration: 2024_03_13_api_keys.py
- POST /webhooks/zapier — Bearer token auth, returns {ideas: [...5], scripts: [...5]}
- Settings page: Integrations section with generate/regenerate key, double-click confirm, 30s full-key display

**Content pages:**
- /faq — 35 questions, 8 sections, accordion, live search/filter, anchor links
- /help — Full tutorial (9 sections), sticky sidebar TOC with scroll-spy, tier tables, callout boxes
- Dashboard dropdown: Help Guide + FAQ + Email Support (was just "Help & Support")

### Architecture Notes
- api_keys table: user_id is UNIQUE (one key per user, upsert on regenerate)
- Workspace switcher: 100% client-side localStorage, no DB changes needed
- PDF generation: reportlab>=4.0.0 in requirements.txt, no OS deps (works on Railway)
- Zapier response: two arrays — scripts (hook/intro/body/cta) and ideas (title/hook/caption/hashtags)

### Reminder Set
- Tomorrow 9AM NL time (8AM UTC): review pytrends data scraping and rate limiting success — check Railway logs for nightly scheduler results

---

## Late Session (20:16–22:00 UTC) — Bug Fixes & Polish

### Commits
- `6a7b35d` — FAQ header collision fix: flex-shrink, white-space:nowrap, 480px breakpoint
- `a973619` — FAQ header mobile fix v2: raised to 640px breakpoint, hid Help Guide btn, shrunk logo to 1rem/28px
- `bdf423c` — Dashboard account dropdown: scrollable on mobile (max-height, overflow-y:auto, overscroll-behavior:contain, thin scrollbar, 100svh mobile)
- `db0caed` — FAQ + Help Guide correction: Tweaks regenerates ALL 5 ideas (not single), updated both templates

### Key Corrections to Product Knowledge
- **Tweaks = full batch regeneration**: "Generate 5 Fresh Ideas with Tweaks" regenerates ALL 5 ideas with fresh angles. NOT a single-idea regeneration. FAQ and help guide now corrected.

### Current State at Chat Reset
**Latest commit:** `db0caed` on main branch
**Railway:** Auto-deploying from GitHub main — all features live
**Pending DB migrations (auto-run on deploy):**
  - 2024_03_13_brand_settings (brand_name/color/logo on creator_profiles)
  - 2024_03_13_api_keys (api_keys table for Zapier)

**All Studio features confirmed in place:**
  - ✅ 90-day calendar (backend + UI lock for non-Studio)
  - ✅ Daily trend digest email (scheduler)
  - ✅ Workspace switcher (5 slots, localStorage)
  - ✅ White-label PDF exports (scripts, hooks, content-pack, calendar)
  - ✅ Zapier webhook (/webhooks/zapier, returns scripts+ideas)

**Content pages live:**
  - /faq — accordion FAQ, live search, 8 sections
  - /help — full tutorial, sticky TOC, 9 sections
  - /settings → Integrations — API key management

**Reminder still active:** Tomorrow 9AM NL / 8AM UTC — review pytrends nightly scheduler results in Railway logs
